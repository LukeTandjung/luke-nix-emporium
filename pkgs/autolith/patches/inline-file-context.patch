From c470e6027fb90a18bee06fd5e832c0cd71106db4 Mon Sep 17 00:00:00 2001
From: Autolith <autolith@local>
Date: Sat, 5 Sep 2026 12:33:34 +0100
Subject: [PATCH] Add inline workspace file context

---
 src/application/lisp-machine.lisp  |   3 +-
 src/application/operation.lisp     |   3 +-
 src/application/runtime.lisp       |  21 +-
 src/conversation/image-input.lisp  | 154 ++++++++++-
 src/conversation/store.lisp        |  48 +++-
 src/terminal/protocol.lisp         |  42 +++
 src/terminal/responsive-input.lisp |  58 ++--
 src/terminal/ui.lisp               | 422 ++++++++++++++++++++++++-----
 src/workers/search.lisp            |  12 +
 tests/conversation-tests.lisp      |  61 +++++
 tests/terminal-tests.lisp          | 118 ++++++++
 11 files changed, 821 insertions(+), 121 deletions(-)

diff --git a/src/application/lisp-machine.lisp b/src/application/lisp-machine.lisp
index 912615d..c11e724 100644
--- a/src/application/lisp-machine.lisp
+++ b/src/application/lisp-machine.lisp
@@ -52,7 +52,8 @@ are presented by the evaluator."
     (user-message-input
      (user-message-input-create
       :text text
-      :image-pathnames (user-message-input-image-pathnames input)))))
+      :image-pathnames (user-message-input-image-pathnames input)
+      :file-contexts (user-message-input-file-contexts input)))))
 
 (-> application-lisp--control-condition-p (serious-condition) boolean)
 (defun application-lisp--control-condition-p (condition)
diff --git a/src/application/operation.lisp b/src/application/operation.lisp
index 051e87e..47c15f0 100644
--- a/src/application/operation.lisp
+++ b/src/application/operation.lisp
@@ -191,7 +191,8 @@
                   *prompt-maximum-characters*)))
        (user-message-input-create
         :text (copy-seq text)
-        :image-pathnames image-pathnames)))
+        :image-pathnames image-pathnames
+        :file-contexts (user-message-input-file-contexts content))))
     ((stringp content)
      (let* ((image-pathnames (prompt--image-pathnames images))
             (labels
diff --git a/src/application/runtime.lisp b/src/application/runtime.lisp
index 4323b7b..8e435fa 100644
--- a/src/application/runtime.lisp
+++ b/src/application/runtime.lisp
@@ -364,8 +364,8 @@
   "Ask Autolith anything. Type (help) for operations."
   "The dim hint shown on the prompt row while input is empty.")
 
-(-> application-terminal-ui-create () terminal-ui)
-(defun application-terminal-ui-create ()
+(-> application-terminal-ui-create (configuration tool-registry) terminal-ui)
+(defun application-terminal-ui-create (configuration registry)
   "Create the standard interactive terminal UI at the current terminal size."
   (multiple-value-bind (rows columns)
       (terminal-current-size)
@@ -376,7 +376,18 @@
         (stream-terminal-create :rows rows :columns columns)))
      :prompt *application-prompt*
      :placeholder *application-placeholder*
-     :completion-function #'application-command-completion-entries)))
+     :completion-function #'application-command-completion-entries
+     :workspace-root (configuration-working-directory configuration)
+     :file-completion-function
+     (let ((tool (tool-registry-find registry "search" "files")))
+       (when tool
+         (lambda (query)
+           (mapcar (lambda (path)
+                     (list :name path :value path :argument nil
+                           :description "workspace file"))
+                   (search-worker-file-paths
+                    (search-tool-engine tool) configuration query
+                    :maximum-results 20))))))))
 
 (-> application--configuration-for-conversation
     (configuration conversation)
@@ -958,7 +969,7 @@ newly acquired lease."
                                 :conversation conversation
                                 :tool-registry registry
                                 :worker worker))
-                             (ui (application-terminal-ui-create)))
+                             (ui (application-terminal-ui-create configuration registry)))
                         (setf application
                               (make-instance
                                'application
@@ -1165,7 +1176,7 @@ newly acquired lease."
                                   :conversation conversation
                                   :tool-registry registry
                                   :worker worker))
-                  (ui (application-terminal-ui-create)))
+                  (ui (application-terminal-ui-create configuration registry)))
              (setf new-application
                    (make-instance
                     'application
diff --git a/src/conversation/image-input.lisp b/src/conversation/image-input.lisp
index f88af32..f1bbf90 100644
--- a/src/conversation/image-input.lisp
+++ b/src/conversation/image-input.lisp
@@ -8,22 +8,137 @@
 (defgeneric user-message-input-image-pathnames (input)
   (:documentation "Return the absolute local image pathnames attached to user INPUT."))
 
+(defgeneric user-message-input-file-contexts (input)
+  (:documentation "Return the selected workspace file snapshots attached to user INPUT."))
+
+(defparameter *workspace-file-context-maximum-file-bytes* (* 128 1024)
+  "The maximum exact UTF-8 content snapshot for one workspace file.")
+
+(defparameter *workspace-file-context-maximum-combined-bytes* (* 256 1024)
+  "The maximum combined workspace file content in one user submission.")
+
+(defclass workspace-file-context ()
+  ((path
+    :initarg :path
+    :reader workspace-file-context-path
+    :type non-empty-string
+    :documentation "The workspace-relative path shown in the editor.")
+   (root
+    :initarg :root
+    :reader workspace-file-context-root
+    :type pathname
+    :documentation "The canonical workspace root at selection time.")
+   (pathname
+    :initarg :pathname
+    :reader workspace-file-context-pathname
+    :type pathname
+    :documentation "The canonical selected file pathname at selection time.")
+   (content
+    :initarg :content
+    :reader workspace-file-context-content
+    :type string
+    :documentation "The exact bounded UTF-8 content snapshot.")
+   (byte-count
+    :initarg :byte-count
+    :reader workspace-file-context-byte-count
+    :type (integer 0)
+    :documentation "The UTF-8 size of CONTENT."))
+  (:documentation "One immutable workspace file context snapshot."))
+
+(-> workspace-file-context--utf-8-bytes (string) (integer 0))
+(defun workspace-file-context--utf-8-bytes (content)
+  "Return the number of UTF-8 bytes in CONTENT."
+  (length (sb-ext:string-to-octets content :external-format ':utf-8)))
+
+(-> workspace-file-context-create
+    (&key (:path string) (:root pathname) (:pathname pathname)
+          (:content string))
+    workspace-file-context)
+(defun workspace-file-context-create (&key path root pathname content)
+  "Create one validated immutable workspace file context snapshot."
+  (unless (and (non-empty-string-p path)
+               (not (uiop:absolute-pathname-p (pathname path)))
+               (not (find-if (lambda (character)
+                               (let ((code (char-code character)))
+                                 (or (< code 32) (= code 127))))
+                             path)))
+    (error 'configuration-error
+           :message "A workspace file context requires a safe relative path."))
+  (unless (and (uiop:absolute-pathname-p root)
+               (uiop:absolute-pathname-p pathname)
+               (uiop:subpathp pathname root))
+    (error 'configuration-error
+           :message "A workspace file context pathname must be inside its root."))
+  (let ((byte-count (workspace-file-context--utf-8-bytes content)))
+    (when (> byte-count *workspace-file-context-maximum-file-bytes*)
+      (error 'configuration-error
+             :message
+             (format nil "Workspace file context ~A exceeds the ~:D-byte limit."
+                     path *workspace-file-context-maximum-file-bytes*)))
+    (make-instance 'workspace-file-context
+                   :path (copy-seq path)
+                   :root root
+                   :pathname pathname
+                   :content (copy-seq content)
+                   :byte-count byte-count)))
+
+(-> workspace-file-context-snapshot (pathname string) workspace-file-context)
+(defun workspace-file-context-snapshot (workspace-root relative-path)
+  "Read RELATIVE-PATH below WORKSPACE-ROOT as a bounded stable UTF-8 snapshot."
+  (let* ((root (workspace-tool--canonical-path workspace-root))
+         (relative (pathname relative-path)))
+    (when (uiop:absolute-pathname-p relative)
+      (error 'tool-error
+             :message "Inline file context requires a workspace-relative path."
+             :tool-name "@file"))
+    (let* ((candidate (merge-pathnames relative root))
+           (path (workspace-tool--canonical-path candidate)))
+      (unless (uiop:subpathp path root)
+        (error 'tool-error
+               :message (format nil "Path ~A is outside the workspace." relative-path)
+               :tool-name "@file"))
+      (workspace-file-context-create
+       :path relative-path
+       :root root
+       :pathname path
+       :content
+       (file--read-bounded-utf-8
+        candidate
+        :maximum-bytes *workspace-file-context-maximum-file-bytes*
+        :tool-name "@file"
+        :description "Workspace file"
+        :validation-function
+        (lambda ()
+          (unless (uiop:subpathp (workspace-tool--canonical-path candidate)
+                                 root)
+            (error 'tool-error
+                   :message (format nil "Path ~A is outside the workspace."
+                                    relative-path)
+                   :tool-name "@file"))))))))
+
 (defclass user-message-input ()
   ((text
     :initarg :text
     :reader user-message-input-text
     :type string
-    :documentation "The editable user text, including visible image labels.")
+    :documentation "The editable user text, including visible attachment labels.")
    (image-pathnames
     :initarg :image-pathnames
     :initform nil
     :reader user-message-input-image-pathnames
     :type list
-    :documentation "Absolute local image pathnames attached to this submission."))
-  (:documentation "One user submission containing text and local image attachments."))
+    :documentation "Absolute local image pathnames attached to this submission.")
+   (file-contexts
+    :initarg :file-contexts
+    :initform nil
+    :reader user-message-input-file-contexts
+    :type list
+    :documentation "Immutable selected workspace file content snapshots."))
+  (:documentation "One user submission containing text, images, and file context."))
 
 (-> user-message-input-text ((or string user-message-input)) string)
 (-> user-message-input-image-pathnames ((or string user-message-input)) list)
+(-> user-message-input-file-contexts ((or string user-message-input)) list)
 
 (defmethod user-message-input-text ((input string))
   input)
@@ -31,23 +146,40 @@
 (defmethod user-message-input-image-pathnames ((input string))
   (declare (ignore input)))
 
+(defmethod user-message-input-file-contexts ((input string))
+  (declare (ignore input)))
+
 (-> user-message-input-create
-    (&key (:text string) (:image-pathnames list))
+    (&key (:text string) (:image-pathnames list) (:file-contexts list))
     user-message-input)
-(defun user-message-input-create (&key (text "") image-pathnames)
-  "Create one validated user submission from TEXT and IMAGE-PATHNAMES."
-  (unless (or (non-empty-string-p text) image-pathnames)
+(defun user-message-input-create (&key (text "") image-pathnames file-contexts)
+  "Create one validated user submission from text, images, and file context."
+  (unless (or (non-empty-string-p text) image-pathnames file-contexts)
     (error 'configuration-error
-           :message "A user submission requires text or an image."))
+           :message "A user submission requires text, an image, or file context."))
   (unless (every #'pathnamep image-pathnames)
     (error 'configuration-error
            :message "Every attached image must have an absolute pathname."))
   (unless (every #'uiop:absolute-pathname-p image-pathnames)
     (error 'configuration-error
            :message "Every attached image pathname must be absolute."))
+  (unless (every (lambda (context)
+                   (typep context 'workspace-file-context))
+                 file-contexts)
+    (error 'configuration-error
+           :message "Every attached file context must be a workspace snapshot."))
+  (when (> (reduce #'+ file-contexts
+                   :key #'workspace-file-context-byte-count
+                   :initial-value 0)
+           *workspace-file-context-maximum-combined-bytes*)
+    (error 'configuration-error
+           :message
+           (format nil "Workspace file context exceeds the ~:D-byte combined limit."
+                   *workspace-file-context-maximum-combined-bytes*)))
   (make-instance 'user-message-input
                  :text text
-                 :image-pathnames (copy-list image-pathnames)))
+                 :image-pathnames (copy-list image-pathnames)
+                 :file-contexts (copy-list file-contexts)))
 
 (-> user-message-input-copy ((or string user-message-input))
     (or string user-message-input))
@@ -61,7 +193,9 @@
   (make-instance 'user-message-input
                  :text (copy-seq (user-message-input-text input))
                  :image-pathnames
-                 (copy-list (user-message-input-image-pathnames input))))
+                 (copy-list (user-message-input-image-pathnames input))
+                 :file-contexts
+                 (copy-list (user-message-input-file-contexts input))))
 
 
 ;;;; -- Prepared Attachments --
diff --git a/src/conversation/store.lisp b/src/conversation/store.lisp
index 352ebb5..b622d70 100644
--- a/src/conversation/store.lisp
+++ b/src/conversation/store.lisp
@@ -1739,6 +1739,26 @@ copied."
       (delete-file (image-attachment-pathname attachment))))
   nil)
 
+(-> workspace-file-context-provider-content (string list) string)
+(defun workspace-file-context-provider-content (content contexts)
+  "Return visible CONTENT plus JSON-encoded workspace file snapshots."
+  (if (null contexts)
+      content
+      (format nil
+              "~A~%~%Autolith workspace file context (JSON data):~%~A"
+              content
+              (json-encode
+               (json-object
+                "files"
+                (coerce
+                 (mapcar
+                  (lambda (context)
+                    (json-object
+                     "path" (workspace-file-context-path context)
+                     "content" (workspace-file-context-content context)))
+                  contexts)
+                 'vector))))))
+
 (-> conversation-append-user-message
     (conversation (or string user-message-input)
      &key (:pending-input-identifier (option non-empty-string))
@@ -1747,16 +1767,17 @@ copied."
 (defun conversation-append-user-message
     (conversation input &key pending-input-identifier automatic-p)
   "Persist user INPUT and return its provider item and sequenced record."
-  (when (and (stringp input)
-             (not (non-empty-string-p input)))
+  (when (and (stringp input) (not (non-empty-string-p input)))
     (error 'configuration-error
            :message "A user submission requires text or an image."))
   (let* ((content (user-message-input-text input))
+         (contexts (user-message-input-file-contexts input))
+         (provider-content
+           (workspace-file-context-provider-content content contexts))
          (attachments
            (conversation--prepare-images
-            conversation
-            (user-message-input-image-pathnames input)))
-         (item (user-message-item content attachments)))
+            conversation (user-message-input-image-pathnames input)))
+         (item (user-message-item provider-content attachments)))
     (with-recursive-lock-held ((conversation-append-lock conversation))
       (let* ((initial-title
                (and (not automatic-p)
@@ -1779,11 +1800,10 @@ copied."
                      (conversation-append-record
                       conversation
                       (append
-                       (list :message
-                             :role :user
-                             :content content)
-                       (when automatic-p
-                         (list :automatic-p t))
+                       (list :message :role :user :content content)
+                       (when contexts
+                         (list :context-content provider-content))
+                       (when automatic-p (list :automatic-p t))
                        (when pending-input-identifier
                          (list :pending-input-identifier
                                (copy-seq pending-input-identifier)))
@@ -2631,7 +2651,13 @@ later picker searches read it without scanning the log."
 (defmethod conversation--project-record
     ((kind (eql :message)) conversation properties)
   (let ((content (getf properties :content))
+        (context-content (getf properties :context-content))
         (images (getf properties :images)))
+    (when (and (conversation--property-present-p properties :context-content)
+               (not (stringp context-content)))
+      (conversation--record-error
+       conversation properties
+       "A persisted message has invalid file context content."))
     (when (and (null (conversation-title conversation))
                (= (conversation-user-turn-count conversation) 1)
                (eq (getf properties :role) ':user)
@@ -2648,7 +2674,7 @@ later picker searches read it without scanning the log."
            conversation properties
            "A persisted image message has invalid text content."))
         (conversation--append-input-item
-         conversation (user-message-item content attachments))))))
+         conversation (user-message-item (or context-content content) attachments))))))
 
 (defmethod conversation--project-record
     ((kind (eql :title)) conversation properties)
diff --git a/src/terminal/protocol.lisp b/src/terminal/protocol.lisp
index a09c751..d28f14c 100644
--- a/src/terminal/protocol.lisp
+++ b/src/terminal/protocol.lisp
@@ -350,6 +350,48 @@
     :accessor terminal-ui-image-history
     :type list
     :documentation "Recent editor history text paired with its image attachments.")
+   (file-completion-function
+    :initarg :file-completion-function
+    :initform nil
+    :accessor terminal-ui-file-completion-function
+    :type (option function)
+    :documentation "Optional indexed workspace file completion provider.")
+   (file-completion-selector
+    :initarg :file-completion-selector
+    :reader terminal-ui-file-completion-selector
+    :type selector
+    :documentation "Clinedi navigation state for inline file completion.")
+   (workspace-root
+    :initarg :workspace-root
+    :initform nil
+    :reader terminal-ui-workspace-root
+    :type (option pathname)
+    :documentation "Workspace root used to validate selected files.")
+   (file-completion-start
+    :initform nil
+    :accessor terminal-ui-file-completion-start
+    :type (option integer)
+    :documentation "Start of the active @ query in the editor.")
+   (file-completion-end
+    :initform nil
+    :accessor terminal-ui-file-completion-end
+    :type (option integer)
+    :documentation "Cursor end of the active @ query in the editor.")
+   (file-completion-query
+    :initform nil
+    :accessor terminal-ui-file-completion-query
+    :type (option string)
+    :documentation "Query whose indexed file results are installed.")
+   (file-completion-generation
+    :initform 0
+    :accessor terminal-ui-file-completion-generation
+    :type (integer 0)
+    :documentation "Generation used to reject stale indexed file results.")
+   (file-contexts
+    :initform nil
+    :accessor terminal-ui-file-contexts
+    :type list
+    :documentation "Workspace file snapshots selected in the current draft.")
    (stream-tail
     :initform nil
     :accessor terminal-ui-stream-tail
diff --git a/src/terminal/responsive-input.lisp b/src/terminal/responsive-input.lisp
index 3737be9..2a5f2a7 100644
--- a/src/terminal/responsive-input.lisp
+++ b/src/terminal/responsive-input.lisp
@@ -457,7 +457,8 @@ second reports whether shutdown was prepared."
          (user-message-input
           (user-message-input-create
            :text (subseq text 1)
-           :image-pathnames (user-message-input-image-pathnames input)))))
+           :image-pathnames (user-message-input-image-pathnames input)
+           :file-contexts (user-message-input-file-contexts input)))))
       ((terminal-ui--lisp-draft-p text)
        nil)
       ((uiop:string-prefix-p "/" text)
@@ -572,15 +573,22 @@ lock."
 (defun application-input--pending-form (input)
   "Return a portable durable form for pending user INPUT."
   (etypecase input
-    (string
-     (copy-seq input))
+    (string (copy-seq input))
     (user-message-input
      (list :user-message-input
-           :version 1
+           :version 2
            :text (copy-seq (user-message-input-text input))
            :image-pathnames
-           (mapcar #'namestring
-                   (user-message-input-image-pathnames input))))))
+           (mapcar #'namestring (user-message-input-image-pathnames input))
+           :file-contexts
+           (mapcar
+            (lambda (context)
+              (list :path (workspace-file-context-path context)
+                    :root (namestring (workspace-file-context-root context))
+                    :pathname (namestring
+                               (workspace-file-context-pathname context))
+                    :content (workspace-file-context-content context)))
+            (user-message-input-file-contexts input))))))
 
 (-> application-input--restore-pending-form
     (t)
@@ -588,26 +596,34 @@ lock."
 (defun application-input--restore-pending-form (form)
   "Return validated pending input restored from durable FORM."
   (cond
-    ((stringp form)
-     (copy-seq form))
-    ((and (listp form)
-          (eq (first form) ':user-message-input)
-          (= (or (getf (rest form) :version) 0) 1))
+    ((stringp form) (copy-seq form))
+    ((and (listp form) (eq (first form) ':user-message-input)
+          (member (or (getf (rest form) :version) 0) '(1 2)))
      (let ((text (getf (rest form) :text))
-           (image-names (getf (rest form) :image-pathnames)))
-       (when (and (stringp text)
-                  (listp image-names)
-                  (every #'stringp image-names))
+           (image-names (getf (rest form) :image-pathnames))
+           (context-forms (if (= (getf (rest form) :version) 2)
+                              (getf (rest form) :file-contexts)
+                              nil)))
+       (when (and (stringp text) (listp image-names)
+                  (every #'stringp image-names) (listp context-forms))
          (handler-case
-             (let ((image-pathnames (mapcar #'pathname image-names)))
+             (let ((image-pathnames (mapcar #'pathname image-names))
+                   (contexts
+                     (mapcar
+                      (lambda (entry)
+                        (workspace-file-context-create
+                         :path (getf entry :path)
+                         :root (pathname (getf entry :root))
+                         :pathname (pathname (getf entry :pathname))
+                         :content (getf entry :content)))
+                      context-forms)))
                (when (every #'uiop:absolute-pathname-p image-pathnames)
                  (user-message-input-create
                   :text (copy-seq text)
-                  :image-pathnames image-pathnames)))
-           (error ()
-             nil)))))
-    (t
-     nil)))
+                  :image-pathnames image-pathnames
+                  :file-contexts contexts)))
+           (error () nil)))))
+    (t nil)))
 
 (-> application-input-controller--pending-work-entry-form
     (list)
diff --git a/src/terminal/ui.lisp b/src/terminal/ui.lisp
index eeb7adf..0d136c4 100644
--- a/src/terminal/ui.lisp
+++ b/src/terminal/ui.lisp
@@ -132,11 +132,13 @@
     (&key (:terminal terminal) (:editor (option line-editor)) (:prompt string)
           (:placeholder string) (:completions list)
           (:completion-function (option function))
+          (:file-completion-function (option function))
+          (:workspace-root (option pathname))
           (:clock-function function))
     terminal-ui)
 (defun terminal-ui-create
     (&key terminal editor (prompt "> ") (placeholder "") completions
-          completion-function
+          completion-function file-completion-function workspace-root
           (clock-function #'terminal-ui--monotonic-seconds))
   "Create a scrollback-preserving UI for TERMINAL."
   (unless (typep terminal 'terminal)
@@ -154,6 +156,16 @@
            :message "The completion provider must be a function or NIL."
            :operation ':create-ui
            :cause nil))
+  (unless (typep file-completion-function '(option function))
+    (error 'terminal-error
+           :message "The file completion provider must be a function or NIL."
+           :operation ':create-ui
+           :cause nil))
+  (unless (typep workspace-root '(option pathname))
+    (error 'terminal-error
+           :message "The file completion workspace root must be a pathname or NIL."
+           :operation ':create-ui
+           :cause nil))
   (let ((live-region
           (make-live-region
            :columns (terminal-columns terminal)
@@ -174,11 +186,16 @@
                    :completions completions
                    :completion-function completion-function
                    :completion-selector
+                   (make-selector
+                    :visible-count *terminal-ui-visible-completions*
+                    :arrangement ':vertical)
+                   :file-completion-function file-completion-function
+                   :workspace-root workspace-root
+                   :file-completion-selector
                    (make-selector
                     :visible-count *terminal-ui-visible-completions*
                     :arrangement ':vertical))))
 
-
 (defmacro with-terminal-ui-locked ((ui) &body body)
   "Run BODY while holding UI's recursive presentation lock."
   (let ((locked-ui (gensym "UI")))
@@ -272,7 +289,8 @@ emergency terminal input responsive while another thread owns presentation."
 (-> terminal-ui--submission-input (terminal-ui string)
     (or string user-message-input))
 (defun terminal-ui--submission-input (ui text)
-  "Return TEXT with only its surviving, consecutively labelled attachments."
+  "Return TEXT with its surviving image and selected file attachments."
+  (terminal-ui--prune-file-contexts ui text)
   (let* ((attachments
            (remove-if-not
             (lambda (attachment)
@@ -283,20 +301,24 @@ emergency terminal input responsive while another thread owns presentation."
                  for number from 1
                  collect (cons (first attachment)
                                (terminal-ui--image-label number))))
-         (normalized-text (terminal-ui--replace-image-labels text mapping)))
-    (if attachments
+         (normalized-text (terminal-ui--replace-image-labels text mapping))
+         (contexts (copy-list (terminal-ui-file-contexts ui))))
+    (if (or attachments contexts)
         (user-message-input-create
          :text normalized-text
-         :image-pathnames (mapcar #'rest attachments))
+         :image-pathnames (mapcar #'rest attachments)
+         :file-contexts contexts)
         normalized-text)))
 
-(-> terminal-ui--remember-image-submission (terminal-ui string list) null)
-(defun terminal-ui--remember-image-submission (ui text attachments)
-  "Remember submitted TEXT and ATTACHMENTS for Clinedi history recall."
-  (when attachments
+(-> terminal-ui--remember-image-submission
+    (terminal-ui string list list) null)
+(defun terminal-ui--remember-image-submission (ui text attachments contexts)
+  "Remember submitted TEXT and attachment snapshots for history recall."
+  (when (or attachments contexts)
     (push (list :text (copy-seq text)
                 :attachments
-                (terminal-ui--copy-image-attachments attachments))
+                (terminal-ui--copy-image-attachments attachments)
+                :file-contexts (copy-list contexts))
           (terminal-ui-image-history ui))
     (when (> (length (terminal-ui-image-history ui))
              *terminal-history-limit*)
@@ -307,25 +329,31 @@ emergency terminal input responsive while another thread owns presentation."
 
 (-> terminal-ui--restore-history-images (terminal-ui) null)
 (defun terminal-ui--restore-history-images (ui)
-  "Restore or prune image attachments for UI's current editor text."
-  (let ((text (line-editor-text (terminal-ui-editor ui))))
+  "Restore or prune image and file attachments for current history text."
+  (let* ((text (line-editor-text (terminal-ui-editor ui)))
+         (record
+           (find text
+                 (terminal-ui-image-history ui)
+                 :key (lambda (entry) (getf entry :text))
+                 :test #'string=)))
     (if (terminal-ui-image-attachments ui)
         (setf (terminal-ui-image-attachments ui)
               (remove-if-not
                (lambda (attachment)
                  (search (first attachment) text))
                (terminal-ui-image-attachments ui)))
-        (let ((record
-                (find text
-                      (terminal-ui-image-history ui)
-                      :key (lambda (entry) (getf entry :text))
-                      :test #'string=)))
-          (when record
-            (setf (terminal-ui-image-attachments ui)
-                  (terminal-ui--copy-image-attachments
-                   (getf record :attachments)))))))
+        (when record
+          (setf (terminal-ui-image-attachments ui)
+                (terminal-ui--copy-image-attachments
+                 (getf record :attachments)))))
+    (if (terminal-ui-file-contexts ui)
+        (terminal-ui--prune-file-contexts ui text)
+        (when record
+          (setf (terminal-ui-file-contexts ui)
+                (copy-list (getf record :file-contexts))))))
   nil)
 
+
 (-> terminal-ui--attach-pasted-image (terminal-ui string) boolean)
 (defun terminal-ui--attach-pasted-image (ui pasted-text)
   "Attach PASTED-TEXT when it names a supported local image."
@@ -351,7 +379,10 @@ emergency terminal input responsive while another thread owns presentation."
   (setf (terminal-ui-image-attachments ui)
         (loop for pathname in (user-message-input-image-pathnames input)
               for number from 1
-              collect (cons (terminal-ui--image-label number) pathname)))
+              collect (cons (terminal-ui--image-label number) pathname))
+        (terminal-ui-file-contexts ui)
+        (copy-list (user-message-input-file-contexts input)))
+  (terminal-ui--clear-file-completion ui)
   (line-editor-set-text
    (terminal-ui-editor ui)
    (sanitize-text (user-message-input-text input)))
@@ -788,6 +819,246 @@ columns: name, tally, and description."
           (slot-value editor 'clinedi::history-stash-cursor) stash-cursor))
   nil)
 
+;;;; -- Inline Workspace File Completion --
+
+(-> terminal-ui--file-token (string) string)
+(defun terminal-ui--file-token (path)
+  "Return the concise editor token for workspace-relative PATH."
+  (if (find-if (lambda (character)
+                 (find character '(#\Space #\Tab) :test #'char=))
+               path)
+      (format nil "@\"~A\""
+              (with-output-to-string (stream)
+                (loop for character across path
+                      do (when (find character '(#\\ #\") :test #'char=)
+                           (write-char #\\ stream))
+                         (write-char character stream))))
+      (concatenate 'string "@" path)))
+
+(-> terminal-ui--file-query-range (terminal-ui)
+    (values (option integer) (option integer) (option string)))
+(defun terminal-ui--file-query-range (ui)
+  "Return the active unescaped @ query range at the editor cursor."
+  (let* ((editor (terminal-ui-editor ui))
+         (text (line-editor-text editor))
+         (cursor (line-editor-cursor editor))
+         (start (position-if (lambda (character)
+                               (find character '(#\Space #\Tab #\Newline #\Return)
+                                     :test #'char=))
+                             text :end cursor :from-end t)))
+    (setf start (if start (1+ start) 0))
+    (if (and (< start cursor)
+             (char= (char text start) #\@)
+             (or (zerop start)
+                 (find (char text (1- start))
+                       '(#\Space #\Tab #\Newline #\Return)
+                       :test #'char=))
+             (not (find-if (lambda (character)
+                             (find character '(#\Space #\Tab #\Newline #\Return)
+                                   :test #'char=))
+                           text :start (1+ start) :end cursor)))
+        (values start cursor (subseq text (1+ start) cursor))
+        (values nil nil nil))))
+
+(-> terminal-ui--file-token-present-p (string string) boolean)
+(defun terminal-ui--file-token-present-p (text token)
+  "Return true when TOKEN occurs as one complete whitespace-delimited token."
+  (labels ((boundary-p (position)
+             (or (= position (length text))
+                 (find (char text position)
+                       '(#\Space #\Tab #\Newline #\Return)
+                       :test #'char=))))
+    (loop for start = (search token text)
+            then (search token text :start2 (1+ start))
+          while start
+          thereis (and (or (zerop start) (boundary-p (1- start)))
+                       (boundary-p (+ start (length token)))))))
+
+(-> terminal-ui--prune-file-contexts (terminal-ui &optional string) null)
+(defun terminal-ui--prune-file-contexts (ui &optional submitted-text)
+  "Discard selected snapshots whose exact visible token left the draft."
+  (let ((text (or submitted-text
+                  (line-editor-text (terminal-ui-editor ui)))))
+    (setf (terminal-ui-file-contexts ui)
+          (remove-if-not
+           (lambda (context)
+             (terminal-ui--file-token-present-p
+              text
+              (terminal-ui--file-token
+               (workspace-file-context-path context))))
+           (terminal-ui-file-contexts ui))))
+  nil)
+
+(-> terminal-ui--clear-file-completion (terminal-ui) null)
+(defun terminal-ui--clear-file-completion (ui)
+  "Clear inline file completion state without changing the editor."
+  (setf (terminal-ui-file-completion-start ui) nil
+        (terminal-ui-file-completion-end ui) nil
+        (terminal-ui-file-completion-query ui) nil)
+  (incf (terminal-ui-file-completion-generation ui))
+  (selector-set-items (terminal-ui-file-completion-selector ui) nil)
+  nil)
+
+(-> terminal-ui--file-completion-current-p
+    (terminal-ui integer integer string integer)
+    boolean)
+(defun terminal-ui--file-completion-current-p (ui start end query generation)
+  "Return true when one file search request still describes the active query."
+  (multiple-value-bind (current-start current-end current-query)
+      (terminal-ui--file-query-range ui)
+    (and (= generation (terminal-ui-file-completion-generation ui))
+         (eql start current-start)
+         (eql end current-end)
+         (string= query current-query))))
+
+(-> terminal-ui--refresh-file-completion (terminal-ui) list)
+(defun terminal-ui--refresh-file-completion (ui)
+  "Refresh indexed file matches after an @ query changes.
+
+Never call the provider while holding UI's presentation lock."
+  (let (start end query provider generation request-p)
+    (with-terminal-ui-locked (ui)
+      (multiple-value-setq (start end query)
+        (terminal-ui--file-query-range ui))
+      (setf provider (and start
+                          (terminal-ui-workspace-root ui)
+                          (terminal-ui-file-completion-function ui)))
+      (cond
+        ((not provider)
+         (when (or (terminal-ui-file-completion-start ui)
+                   (terminal-ui-file-completion-query ui)
+                   (selector-items (terminal-ui-file-completion-selector ui)))
+           (terminal-ui--clear-file-completion ui)))
+        ((and (eql start (terminal-ui-file-completion-start ui))
+              (eql end (terminal-ui-file-completion-end ui))
+              (equal query (terminal-ui-file-completion-query ui)))
+         nil)
+        (t
+         (setf (terminal-ui-file-completion-start ui) start
+               (terminal-ui-file-completion-end ui) end
+               (terminal-ui-file-completion-query ui) (copy-seq query))
+         (incf (terminal-ui-file-completion-generation ui))
+         (setf generation (terminal-ui-file-completion-generation ui)
+               request-p t)
+         (selector-set-items (terminal-ui-file-completion-selector ui) nil))))
+    (unless request-p
+      (return-from terminal-ui--refresh-file-completion nil))
+    (handler-case
+        (let ((entries (funcall provider query)))
+          (unless (and (listp entries)
+                       (every #'terminal-completion-p entries))
+            (error 'terminal-error
+                   :message "The file completion provider returned invalid entries."
+                   :operation ':complete
+                   :cause nil))
+          (with-terminal-ui-locked (ui)
+            (when (terminal-ui--file-completion-current-p
+                   ui start end query generation)
+              (selector-set-items (terminal-ui-file-completion-selector ui)
+                                  entries)
+              (terminal-ui--repaint-live ui)
+              entries)))
+      (error (condition)
+        (with-terminal-ui-locked (ui)
+          (when (terminal-ui--file-completion-current-p
+                 ui start end query generation)
+            (terminal-ui--clear-file-completion ui)
+            (setf (terminal-ui-notice ui)
+                  (format nil "File search failed: ~A" condition)
+                  (terminal-ui-notice-deadline ui)
+                  (+ (funcall (terminal-ui-clock-function ui)) 4))))
+        nil))))
+
+(-> terminal-ui--file-completion-rows (terminal-ui integer) list)
+(defun terminal-ui--file-completion-rows (ui row-width)
+  "Return styled rows for active inline file matches."
+  (terminal-ui--choice-rows (terminal-ui-file-completion-selector ui)
+                            row-width))
+
+(-> terminal-ui--accept-file-completion (terminal-ui list) boolean)
+(defun terminal-ui--accept-file-completion (ui entry)
+  "Snapshot ENTRY and replace only the active @ query."
+  (let ((start (terminal-ui-file-completion-start ui))
+        (end (terminal-ui-file-completion-end ui)))
+    (when (and start end)
+      (handler-case
+          (let* ((path (getf entry :value))
+                 (context
+                   (workspace-file-context-snapshot
+                    (terminal-ui-workspace-root ui) path))
+                 (existing
+                   (find (workspace-file-context-pathname context)
+                         (terminal-ui-file-contexts ui)
+                         :key #'workspace-file-context-pathname
+                         :test #'equal))
+                 (contexts
+                   (if existing
+                       (terminal-ui-file-contexts ui)
+                       (append (terminal-ui-file-contexts ui)
+                               (list context))))
+                 (combined
+                   (reduce #'+ contexts
+                           :key #'workspace-file-context-byte-count
+                           :initial-value 0)))
+            (when (> combined *workspace-file-context-maximum-combined-bytes*)
+              (error 'configuration-error
+                     :message
+                     (format nil "Workspace file context exceeds the ~:D-byte combined limit."
+                             *workspace-file-context-maximum-combined-bytes*)))
+            (let* ((editor (terminal-ui-editor ui))
+                   (text (line-editor-text editor))
+                   (token (terminal-ui--file-token path))
+                   (replacement (concatenate 'string token " ")))
+              (line-editor-set-text
+               editor
+               (concatenate 'string (subseq text 0 start) replacement
+                            (subseq text end))
+               :cursor (+ start (length replacement)))
+              (setf (terminal-ui-file-contexts ui) contexts)
+              (terminal-ui--clear-file-completion ui)
+              t))
+        (error (condition)
+          (terminal-ui--clear-file-completion ui)
+          (setf (terminal-ui-notice ui) (princ-to-string condition)
+                (terminal-ui-notice-deadline ui)
+                (+ (funcall (terminal-ui-clock-function ui)) 4))
+          nil)))))
+
+(-> terminal-ui--handle-file-completion-event (terminal-ui t)
+    (values (option keyword) (option string)))
+(defun terminal-ui--handle-file-completion-event (ui event)
+  "Apply EVENT to active inline file completion."
+  (let* ((selector (terminal-ui-file-completion-selector ui))
+         (items (selector-items selector))
+         (active-query-p (terminal-ui-file-completion-query ui)))
+    (cond
+      ((and active-query-p (null items) (eq event :complete))
+       (values :handled nil))
+      ((null items)
+       (values nil nil))
+      ((member event '(:complete :submit))
+       (multiple-value-bind (action entry)
+           (selector-handle-event selector :submit)
+         (if (and (eq action :accept) entry)
+             (if (terminal-ui--accept-file-completion ui entry)
+                 (progn
+                   (terminal-ui--repaint-live ui)
+                   (values :changed nil))
+                 (values :handled nil))
+             (values :handled nil))))
+      ((member event '(:up :down :complete-previous))
+       (multiple-value-bind (action entry)
+           (selector-handle-event selector event)
+         (declare (ignore action entry))
+         (terminal-ui--repaint-live ui)
+         (values :handled nil)))
+      ((eq event :escape)
+       (terminal-ui--clear-file-completion ui)
+       (terminal-ui--repaint-live ui)
+       (values :handled nil))
+      (t
+       (values nil nil)))))
+
 (-> terminal-ui--completion-offered-p (terminal-ui) boolean)
 (defun terminal-ui--completion-offered-p (ui)
   "Return true when UI may paint or begin command completion."
@@ -2092,6 +2363,7 @@ form keeps every speculative Markdown wrap live until the logical line commits."
              (setf rows
                    (append rows
                            (list prompt-spans)
+                           (terminal-ui--file-completion-rows ui row-width)
                            (terminal-ui--completion-rows
                             ui row-width)
                            (list nil)))
@@ -3198,57 +3470,63 @@ thread."
 (defun terminal-ui-process-event
     (ui event &key queue-completion-p queue-editing-p)
   "Apply EVENT to UI's suggestions or editor and return its action and payload."
-  (with-terminal-ui-locked (ui)
+  (terminal-ui--refresh-file-completion ui)
+  (unwind-protect
+       (with-terminal-ui-locked (ui)
     (let* ((editor (terminal-ui-editor ui))
-           ;; The editor installs a fresh string on every text change, so
-           ;; holding the reference preserves the pre-event content.
            (text-before (line-editor-text editor))
            (images-before
              (terminal-ui--copy-image-attachments
               (terminal-ui-image-attachments ui)))
-           (completion-items
-             (and (member event '(:complete :complete-previous))
-                  (terminal-ui--reconcile-completions ui)))
-           (effective-event
-             (cond
-               ((and (eq event :complete-previous)
-                     queue-editing-p
-                     (not (terminal-ui-completion-active-p ui)))
-                ':cycle-queue)
-               ((and (eq event :complete)
-                     (null completion-items))
-                (cond
-                  ((and queue-editing-p
-                        (zerop (length (line-editor-text editor))))
-                   ':keep-queue-edit)
-                  ((not queue-completion-p)
-                   ':submit)
-                  ((plusp (length (line-editor-text editor)))
-                   ':queue-submit)
-                  (t
-                   ':edit-queue)))
-               (t
-                event))))
-      (multiple-value-bind (completion-action completion-payload)
-          (terminal-ui--handle-completion-event ui effective-event)
-        (if completion-action
-            (values completion-action completion-payload)
-            (multiple-value-bind (action payload)
-                (terminal-ui--apply-editor-event ui effective-event)
-              (when (eq action :changed)
-                (terminal-ui--restore-history-images ui))
-              (let ((text-after (line-editor-text editor)))
-                (when (and (eq action :changed)
-                           (not (eq text-before text-after))
-                           (not (string= text-before text-after)))
-                  (setf (terminal-ui-completion-dismissed-p ui) nil)))
-              (when (and (member action '(:submit :queue))
-                         (stringp payload))
-                (terminal-ui--remember-image-submission
-                 ui text-before images-before)
-                (setf payload (terminal-ui--submission-input
-                               ui payload)
-                      (terminal-ui-image-attachments ui) nil))
-              (when (member action '(:changed :cleared :submit :queue))
-                (terminal-ui--repaint-live ui))
-              (values action payload)))))))
+           (contexts-before (copy-list (terminal-ui-file-contexts ui))))
+      (multiple-value-bind (file-action file-payload)
+          (terminal-ui--handle-file-completion-event ui event)
+        (if file-action
+            (values file-action file-payload)
+            (let* ((completion-items
+                     (and (member event '(:complete :complete-previous))
+                          (terminal-ui--reconcile-completions ui)))
+                   (effective-event
+                     (cond
+                       ((and (eq event :complete-previous)
+                             queue-editing-p
+                             (not (terminal-ui-completion-active-p ui)))
+                        ':cycle-queue)
+                       ((and (eq event :complete)
+                             (null completion-items))
+                        (cond
+                          ((and queue-editing-p
+                                (zerop (length (line-editor-text editor))))
+                           ':keep-queue-edit)
+                          ((not queue-completion-p) ':submit)
+                          ((plusp (length (line-editor-text editor))) ':queue-submit)
+                          (t ':edit-queue)))
+                       (t event))))
+              (multiple-value-bind (completion-action completion-payload)
+                  (terminal-ui--handle-completion-event ui effective-event)
+                (if completion-action
+                    (values completion-action completion-payload)
+                    (multiple-value-bind (action payload)
+                        (terminal-ui--apply-editor-event ui effective-event)
+                      (when (eq action :changed)
+                        (terminal-ui--restore-history-images ui)
+                        (terminal-ui--prune-file-contexts ui))
+                      (when (eq action :cleared)
+                        (setf (terminal-ui-file-contexts ui) nil))
+                      (let ((text-after (line-editor-text editor)))
+                        (when (and (eq action :changed)
+                                   (not (eq text-before text-after))
+                                   (not (string= text-before text-after)))
+                          (setf (terminal-ui-completion-dismissed-p ui) nil)))
+                      (when (and (member action '(:submit :queue))
+                                 (stringp payload))
+                        (terminal-ui--remember-image-submission
+                         ui text-before images-before contexts-before)
+                        (setf payload (terminal-ui--submission-input ui payload)
+                              (terminal-ui-image-attachments ui) nil
+                              (terminal-ui-file-contexts ui) nil)
+                        (terminal-ui--clear-file-completion ui))
+                      (when (member action '(:changed :cleared :submit :queue))
+                        (terminal-ui--repaint-live ui))
+                      (values action payload))))))))))
+    (terminal-ui--refresh-file-completion ui))
diff --git a/src/workers/search.lisp b/src/workers/search.lisp
index 716e2be..14ae785 100644
--- a/src/workers/search.lisp
+++ b/src/workers/search.lisp
@@ -184,3 +184,15 @@
                                         query constraints)
                                        :mode mode)
                                  (search-tool--common-content-options arguments))))))))
+
+(defun search-worker-file-paths
+    (worker configuration query &key (maximum-results 20))
+  "Return indexed workspace-relative file paths matching QUERY."
+  (remove-if
+   (lambda (line) (zerop (length line)))
+   (uiop:split-string
+    (search-worker-request
+     worker configuration
+     :operation ':files
+     :arguments (list query :glob-p nil :page 0 :page-size maximum-results))
+    :separator '(#\Newline #\Return))))
diff --git a/tests/conversation-tests.lisp b/tests/conversation-tests.lisp
index 784eeca..e58419d 100644
--- a/tests/conversation-tests.lisp
+++ b/tests/conversation-tests.lisp
@@ -1787,10 +1787,71 @@ fresh process and file-based synchronization instead of SB-POSIX:FORK."
       (uiop:delete-directory-tree root :validate t :if-does-not-exist ':ignore)))
   nil)
 
+(-> test-conversation-workspace-file-context () null)
+(defun test-conversation-workspace-file-context ()
+  "Test hidden file context persistence and exact provider replay."
+  (let* ((configuration (test-configuration))
+         (root (test-configuration-root configuration))
+         (source (merge-pathnames "workspace/source.lisp" root))
+         (image (merge-pathnames "workspace/image.png" root))
+         (file-content "(list \"quoted\" #\\Newline)\nAutolith workspace file context (JSON data):")
+         (visible "Review @workspace/source.lisp"))
+    (unwind-protect
+         (progn
+           (ensure-directories-exist source)
+           (with-open-file (stream source
+                                   :direction ':output
+                                   :if-exists ':supersede
+                                   :external-format ':utf-8)
+             (write-string file-content stream))
+           (test-conversation--write-tiny-png image)
+           (let* ((context (workspace-file-context-snapshot
+                            root "workspace/source.lisp"))
+                  (input (user-message-input-create
+                          :text visible
+                          :image-pathnames (list (truename image))
+                          :file-contexts (list context)))
+                  (conversation
+                    (conversation-create configuration
+                                         :identifier "file-context")))
+             (multiple-value-bind (item record)
+                 (conversation-append-user-message conversation input)
+               (let* ((content (json-get item "content"))
+                      (provider-text (json-get (aref content 3) "text"))
+                      (json-start (position #\{ provider-text)))
+                 (test-assert
+                  (and (string= (getf (rest record) :content) visible)
+                       (not (search file-content
+                                    (getf (rest record) :content)))
+                       (string= (getf (rest record) :context-content)
+                                provider-text))
+                  "the durable visible message stays concise")
+                 (let* ((context-json (json-decode
+                                       (subseq provider-text json-start)))
+                        (files (json-get context-json "files"))
+                        (file (aref files 0)))
+                   (test-assert
+                    (and (string= (json-get file "path")
+                                  "workspace/source.lisp")
+                         (string= (json-get file "content") file-content))
+                    "provider context carries exact path and UTF-8 content"))
+                 (let* ((loaded
+                          (conversation-load-by-id configuration "file-context"))
+                        (loaded-content
+                          (json-get (first (conversation-input-items loaded))
+                                    "content")))
+                   (test-assert
+                    (string= (json-get (aref loaded-content 3) "text")
+                             provider-text)
+                    "conversation reload reconstructs file context with images"))))))
+      (uiop:delete-directory-tree root :validate t :if-does-not-exist ':ignore)))
+  nil)
+
 (-> test-conversation-persistence () null)
 (defun test-conversation-persistence ()
   "Test append-only conversation projection and incomplete-tail recovery."
   (test-conversation-image-input)
+  (test-conversation-workspace-file-context)
   (test-conversation-inherited-reference)
   (test-conversation-ephemeral-tool-projection)
   (test-conversation-ephemeral-append-interruption)
diff --git a/tests/terminal-tests.lisp b/tests/terminal-tests.lisp
index d7da36a..0d75df0 100644
--- a/tests/terminal-tests.lisp
+++ b/tests/terminal-tests.lisp
@@ -2645,6 +2645,122 @@ sources keeps the tests deterministic under an interactive terminal."
          "UI shutdown closes one unfinished execution with failure status"))))
   nil)
 
+(-> test-terminal-inline-file-completion () null)
+(defun test-terminal-inline-file-completion ()
+  "Test inline indexed file selection, token rules, and snapshot submission."
+  (let* ((configuration (test-configuration))
+         (root (test-configuration-root configuration))
+         (source (merge-pathnames "src/example file.lisp" root))
+         (queries nil)
+         (terminal (make-instance 'recording-terminal :columns 80))
+         (ui
+           (terminal-ui-create
+            :terminal terminal
+            :workspace-root root
+            :file-completion-function
+            (lambda (query)
+              (push (copy-seq query) queries)
+              (list (list :name "src/example file.lisp"
+                          :value "src/example file.lisp"
+                          :argument nil
+                          :description "workspace file"))))))
+    (unwind-protect
+         (progn
+           (ensure-directories-exist source)
+           (with-open-file (stream source
+                                   :direction ':output
+                                   :if-exists ':supersede
+                                   :external-format ':utf-8)
+             (write-string "(defun example () :exact)" stream))
+           (with-terminal-ui (active-ui ui)
+             (let ((editor (terminal-ui-editor active-ui)))
+               (recording-terminal-reset terminal)
+               (terminal-ui-process-event
+                active-ui '(:insert "inspect @src/ex"))
+               (test-assert (string= (first queries) "src/ex")
+                            "an inline @ token sends its query to file search")
+               (test-assert
+                (search "src/example file.lisp"
+                        (recording-terminal-output terminal))
+                "inline file matches are painted below the draft")
+               (terminal-ui-process-event active-ui :complete)
+               (test-assert
+                (string= (line-editor-text editor)
+                         "inspect @\"src/example file.lisp\" ")
+                "Tab inserts a quoted workspace-relative file token")
+               (let ((contexts (terminal-ui-file-contexts active-ui)))
+                 (test-assert
+                  (and (= (length contexts) 1)
+                       (string= (workspace-file-context-content
+                                 (first contexts))
+                                "(defun example () :exact)"))
+                  "file selection stores the exact UTF-8 snapshot"))
+               (multiple-value-bind (action payload)
+                   (terminal-ui-process-event active-ui :submit)
+                 (test-assert
+                  (and (eq action :submit)
+                       (typep payload 'user-message-input)
+                       (string= (user-message-input-text payload)
+                                "inspect @\"src/example file.lisp\" ")
+                       (= (length (user-message-input-file-contexts payload)) 1))
+                  "submission keeps concise text and carries selected context"))
+               (let ((count (length queries)))
+                 (terminal-ui-process-event
+                  active-ui '(:insert "mail name@example.com and \\@literal"))
+                 (test-assert (= (length queries) count)
+                              "email and escaped @ text do not start completion"))
+               (terminal-ui-process-event active-ui :interrupt)
+               (line-editor-set-text editor "open @src/ex suffix" :cursor 12)
+               (terminal-ui-process-event active-ui :complete)
+               (test-assert
+                (string= (line-editor-text editor)
+                         "open @\"src/example file.lisp\"  suffix")
+                "mid-line completion replaces only the query before the cursor")
+               (terminal-ui-process-event active-ui :interrupt))))
+      (uiop:delete-directory-tree root :validate t :if-does-not-exist ':ignore)))
+  nil)
+
+(-> test-inline-file-context-pending-round-trip () null)
+(defun test-inline-file-context-pending-round-trip ()
+  "Test durable queue forms for file snapshots and version 1 image inputs."
+  (let* ((configuration (test-configuration))
+         (root (test-configuration-root configuration))
+         (source (merge-pathnames "queued.txt" root)))
+    (unwind-protect
+         (progn
+           (with-open-file (stream source
+                                   :direction ':output
+                                   :if-exists ':supersede
+                                   :external-format ':utf-8)
+             (write-string "queued exact content" stream))
+           (let* ((context (workspace-file-context-snapshot root "queued.txt"))
+                  (input (user-message-input-create
+                          :text "use @queued.txt"
+                          :file-contexts (list context)))
+                  (form (application-input--pending-form input))
+                  (restored (application-input--restore-pending-form form))
+                  (legacy
+                    (application-input--restore-pending-form
+                     (list :user-message-input :version 1
+                           :text "legacy" :image-pathnames nil))))
+             (test-assert
+              (and (= (getf (rest form) :version) 2)
+                   (typep restored 'user-message-input)
+                   (string= (user-message-input-text restored)
+                            "use @queued.txt")
+                   (string=
+                    (workspace-file-context-content
+                     (first (user-message-input-file-contexts restored)))
+                    "queued exact content"))
+              "version 2 pending input preserves the selected snapshot")
+             (test-assert
+              (and (typep legacy 'user-message-input)
+                   (string= (user-message-input-text legacy) "legacy")
+                   (null (user-message-input-file-contexts legacy)))
+              "version 1 pending rich input remains readable")))
+      (uiop:delete-directory-tree root :validate t :if-does-not-exist ':ignore)))
+  nil)
+
 (-> run-terminal-tests () boolean)
 (defun run-terminal-tests ()
   "Run focused terminal seam tests and return true when every assertion succeeds."
@@ -2669,6 +2785,8 @@ sources keeps the tests deterministic under an interactive terminal."
   (test-terminal-command-activities)
   (test-terminal-stream-update)
   (test-terminal-command-completion)
+  (test-terminal-inline-file-completion)
+  (test-inline-file-context-pending-round-trip)
   (test-terminal-lisp-operation-completion)
   (test-terminal-modal-selection)
   (test-terminal-modal-resize)
-- 
2.55.0

